# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# signing keys — NEVER commit
*.key
*.key.pub

# local Claude Code tooling (launch configs, etc.)
.claude/

# Cloudflare deploy config (account + KV ids) — keep out of the public repo
diagnostics-worker/wrangler.toml
